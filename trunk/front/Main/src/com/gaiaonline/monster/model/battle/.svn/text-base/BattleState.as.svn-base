//Created by Action Script Viewer - http://www.buraks.com/asv
package com.gaiaonline.monster.model.battle {

    public class BattleState {

        private var _describe:String;

        public static const BEFORE_BATTLE_HAS_STARTED:BattleState = new BattleState("before battle has started");
;
        public static const SET_FIRST_MONSTER_PENDING:BattleState = new BattleState("PENDING: set first monster");
;
        public static const SET_FIRST_MONSTER:BattleState = new BattleState("set first monster");
;
        public static const START_PENDING:BattleState = new BattleState("PENDING: start");
;
        public static const INPUT:BattleState = new BattleState("input");
;
        public static const CHOOSE_NEXT_MONSTER:BattleState = new BattleState("choose next monster");
;
        public static const CHOOSE_NEXT_MONSTER_PENDING:BattleState = new BattleState("PENDING: choose next monster");
;
        public static const CHOOSE_NEXT_MONSTER_SHOWN:BattleState = new BattleState("(SHOWN) choose next monster");
;
        public static const CAPTURE_MONSTER:BattleState = new BattleState("capture monster");
;
        public static const CAPTURE_MONSTER_PENDING:BattleState = new BattleState("PENDING: capture monster");
;
        public static const ATTACK:BattleState = new BattleState("attack");
;
        public static const ATTACK_PENDING:BattleState = new BattleState("PENDING: attack");
;
        public static const USE_ITEM:BattleState = new BattleState("use item");
;
        public static const USE_ITEM_PENDING:BattleState = new BattleState("PENDING: use item");
;
        public static const SWAP_MONSTER:BattleState = new BattleState("swap monster");
;
        public static const SWAP_MONSTER_PENDING:BattleState = new BattleState("PENDING: swap monster");
;
        public static const FORCED_SWAP_MONSTER:BattleState = new BattleState("forced swap monster");
;
        public static const FORCED_SWAP_MONSTER_PENDING:BattleState = new BattleState("PENDING: forced swap monster");
;
        public static const NPC_SWAP:BattleState = new BattleState("npc swap");
;
        public static const RECHARGE_MONSTER:BattleState = new BattleState("recharge monster");
;
        public static const RECHARGE_MONSTER_PENDING:BattleState = new BattleState("PENDING: recharge monster");
;
        public static const RUN_AWAY:BattleState = new BattleState("run away");
;
        public static const RUN_AWAY_PENDING:BattleState = new BattleState("PENDING: run away");
;
        public static const REQUEST_HELP:BattleState = new BattleState("request help");
;
        public static const REQUEST_HELP_PENDING:BattleState = new BattleState("PENDING: request help");
;
        public static const PLAYER_WIN:BattleState = new BattleState("win");
;
        public static const PLAYER_LOSE:BattleState = new BattleState("lose");
;
        public static const END:BattleState = new BattleState("end");
;
        private static const DESCRIBE:String = "[BattleState] ";

        public function BattleState(_arg1:String){
            this._describe = _arg1;
        }
        public function toString():String{
            return ((DESCRIBE + this._describe));
        }

    }
}//package com.gaiaonline.monster.model.battle 
